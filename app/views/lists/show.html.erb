<h1><%= @list.name %></h1>

<ul>
  <% @list.movies.each do |movie| %>
    <li><%= movie.title %>
  <% end %>
</ul>

<%# <%= link_to "Add Bookmark", new_bookmark_path %>
<%= link_to "Back to all lists", lists_path %>
